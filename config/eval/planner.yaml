defaults:
  - pusht
  - override /solver: learned_subgoal
  - _self_

device: cuda
planner_checkpoint: subgoal_pusht/weights_epoch_100.pt
planner_cache_dir: null

eval:
  goal_offset_steps: 50
  eval_budget: 75

output:
  dir: outputs/planner_eval
  filename: pusht_results.txt
